# Repair addendum

Same role as `generation.md` but with the additional context of a
validator failure attached. Read the verbatim validator output and
produce a fix that resolves it without regressing the issue's intent.
Output contract unchanged: column-0 fenced ` ```path=<relpath> ` blocks,
full file contents.
